<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
    <script type="text/javascript" src="http://code.jquery.com/jquery-1.11.3.min.js"></script>
    <script type="text/javascript" src="http://cdn.opalrb.org/opal/0.8.0/opal.min.js"></script>
    <%== assets(:js) %>
    <title>Slab - <%= @title %></title>
  </head>
  
  <body>
    <nav class="navbar navbar-default" role="navigation">
      <div class="container">
        <div class="navbar-header">
          <a href="/upload" class="navbar-brand">Slab</a>
        </div>

        <ul class="nav navbar-nav">
          <li><a href="/documents">Uploaded Documents</a></li>
        </ul>

        <% if rodauth.logged_in? %>
          <form action="/auth/logout" method="post" class="navbar-form">
            <%== csrf_tag %>
            <input type="submit" value="Logout" class="btn btn-warning pull-right form-control">
          </form>
        <% end %>
      </div>
    </nav>

    <div class="container">
      <% if @title %>
        <h1><%= @title %></h1>
      <% end %>
      <%== yield %>
    </div>
  </body>
</html>
